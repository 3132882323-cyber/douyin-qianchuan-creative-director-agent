# V1.4.0 源码发布检查清单

本清单用于发布前复核。自动检查可以在源码环境完成；Chrome 人工验收、打包和 GitHub 发布必须由发布者在可信设备上完成。

## 版本与仓库

- [x] `manifest.json`、`package.json`、侧边栏、工作台、方案与转码默认版本均为 `1.4.0`。
- [x] `LICENSE`、`README.md`、`PRIVACY.md`、`CHANGELOG.md` 均包含在源码包中。
- [x] `git status` 只包含本次预期文件；没有 `.pem`、Token、Cookie、下载素材、本机绝对路径或临时日志。
- [x] 未删除或覆盖用户未提交的无关改动。

## 自动验证

```powershell
npm test
npm run check
```

- [x] Node 单元测试全部通过（250 / 250）。
- [x] 37 个 JavaScript 入口或安全模块通过 `node --check`。
- [x] `tools/transcode-worker.ps1` 通过 PowerShell AST 解析。
- [x] `git diff --check` 无空白错误。
- [x] 静态 HTML ID 唯一，所有 JavaScript 静态 ID 选择器均有对应控件。
- [x] Manifest 权限仍为 `sidePanel`、`storage`；可选主机权限只有固定 GitHub API。
- [x] CSP 精确为 `script-src 'self'; object-src 'none';`，页面没有远程脚本、模型或 WASM。
- [x] 源码不存在 `innerHTML` 写入、`eval`、动态函数或文档写入等执行入口。
- [x] CSV 公式安全、非媒体导入限额、V0.5/V0.6 恢复和损坏存储隔离测试通过。

## Chrome 人工验收

- [ ] 在全新浏览器资料目录“加载已解压”，扩展卡片显示 `1.4.0` 且无 Manifest 错误。
- [ ] 点击扩展图标可打开侧边栏；首次三步引导可收起、重新打开，并保持键盘焦点可见。
- [ ] 在约 280–400 px 窄侧边栏检查四个标签页、恢复提示、引导、表格横向滚动和按钮布局。
- [ ] 导入 `examples/sample-report.csv`，完成字段映射、复盘、生成、编辑、复制和三种导出。
- [ ] 生成四条任务后确认卡片和“复制开拍清单”均按基线 → 变体编号排序；修改钩子、现场任务或剪辑要求后重新复制，清单必须使用最新编辑值，并包含唯一变量、现场必拍、字幕与发布前检查。
- [ ] 完整方案应显示全部任务“开拍项已齐”；分别清空钩子和剪辑要求后，顶部与对应卡片即时列出准确缺项，重新补齐后提示恢复。
- [ ] 缺项时点击“复制开拍清单”必须先出现确认框：取消不写剪贴板、不标记方案已完成，确认后才复制并保留“待补充”提示；完整方案复制不受该确认影响。
- [ ] 确认“复制开拍清单”是方案区唯一主复制动作，“复制完整方案”和 Markdown / CSV / JSON 仍可到达；两种复制都不自动修改任何制作状态。
- [ ] 逐条点击“复制前三秒监看卡”，确认包含 0–1 秒首帧、1–3 秒钩子/口播/字幕、3 秒后证据承接、开机前五问和返拍触发；复制不会标记方案已完成或写入新的存储字段。
- [ ] 清空证据镜头或剪辑承接后，确认对应监看卡按钮禁用并列出准确缺项；重新补齐立即恢复。修改钩子但不修改口播首句时必须显示错位提醒。
- [ ] 输入超长钩子、首屏字幕和首帧分镜，确认仅提示真人盲读、手机停帧和单焦点复核，不出现创意评分、效果结论或自动改写；窄侧栏按钮与提示不得横向溢出。
- [ ] 展开“前三秒拍前盲审”，确认 2–12 条完整同批方案会匿名打乱，且原顺序第一条不会固定成为盲审方案 A；重复复制未编辑方案时盲审编号和映射保持一致。
- [ ] 在测试编号、批次编号、基线素材、钩子、字幕和证据任务中放入可识别字符串、金额及带数值 ROI/CTR/CVR/CPA/GMV/消耗/转化率/点击率/完播率/留存率；公共盲审包不得包含这些身份和表现值，导演映射必须仍可核对真实测试编号。
- [ ] 先复制盲审包，再编辑任一首帧、钩子或字幕，确认盲审编号变化；清空任一方案的证据镜头后三个复制按钮同时禁用。复制公共包、映射或合议单均不得保存反馈、改变方案完成状态或写入新存储字段。
- [ ] 反馈锁定后复制“导演合议单”，确认盲审编号与当前匿名包、导演映射一致；每条方案均包含回收事实、保留开拍/单变量重写/本轮淘汰三选一、选择理由和失败条件。
- [ ] 构造重复或缺失的盲审标签，确认合议单失败关闭；正常合议单不得自动填写票数、胜负或投放结论，复制前后不得保存反馈、修改方案、父版本、制作状态或投放状态。
- [ ] 展开“批次共用镜头板”，使用钩子批次确认先列 B00 全流程母版、共用证据骨架，再列 A01/A02 的 0–3 秒变量插拍；B00 不应被要求重复拍同一变量插条。
- [ ] 分别生成核心主张、拍摄场景和目标受众批次，确认共用规则与重拍范围随变量变化：主张必须重核证据，换场必须重记机位/曝光/收音，受众必须重拍称呼、问题触发与人物反应。
- [ ] 修改任一非测试字段、保持不变项或变量声明，或制造重复变量值，确认镜头板按钮立即禁用并列出准确原因；恢复单变量一致性后重新可复制。
- [ ] 复制镜头板前后检查 `chrome.storage.local`、IndexedDB、方案完成状态和制作状态均不变化；输出不得声称节省具体工时、自动合并素材或自动通过现场连续性。
- [ ] 点击“复制剪辑装配单”，确认包含 00_REFERENCE、10_SHARED、20_VARIANTS、90_EXPORT 素材筐命名，B00 时间轴锁定、逐版本口播/字幕/证据映射、首轮导出验收和明确防串版要求。
- [ ] 钩子、主张、场景和受众批次的变量窗口必须不同；人为制造固定字段漂移后，拍摄镜头板与剪辑装配单按钮应同时禁用。复制前后不得读取媒体目录、移动或改名文件、操作剪辑软件、保存检查结果或改变制作状态。
- [ ] 取消文件选择器、导入错误 CSV / JSON 后，当前成功报表、复盘和方案保持不变。
- [ ] 人工构造单个损坏存储键后重新打开：页面仍可用、面板列出具体键、取消清理不变、确认后只删除列出的键。
- [ ] 使用 V0.5 schema 1 与 V0.6 schema 2 备份分别导入，确认迁移提示与内容正确。
- [ ] 从 V1.1 单工作区升级，确认自动生成第一个 IndexedDB 项目且原创作任务、复盘、方案和导出状态不丢失。
- [ ] 新建第二个项目并往返切换，确认两个项目的数据互不串写；有未完成报表标签或素材工具页面态时必须先提示。
- [ ] 连续生成同主题、同变量方案，确认测试编号不同；选择父版本后时间线显示正确引用。
- [ ] 从旧 V1.3 工作区升级，确认历史版本统一显示“未标记”，不会根据方案、结果、导出状态或时间自动推断为待拍、已上线等状态。
- [ ] 在版本卡依次选择待拍、拍摄中、剪辑中、待投放、已上线、已搁置和未标记；刷新页面、切换项目后确认状态与更新时间仍正确，保存失败时下拉框恢复原值。
- [ ] 分别为新测试起点基线、同批次 B00/A01 变体和跨轮父子基线复制“千川投放交接卡”；确认单变量对照对象、测试编号、目标 ROI、最低消耗、停止条件、达成后动作和结果回填口径准确。
- [ ] 分别构造未到待投放阶段、目标 ROI 为 0、最低消耗为 0、缺停止条件、缺批次、缺同批次基线和同批次多基线；确认均显示具体缺项且不伪装为已就绪。
- [ ] 在不完整交接卡确认框点击取消，确认剪贴板、项目数据和制作状态均不变；再次确认后只复制带“待补充”的文本，不自动补写字段。
- [ ] 复制完整交接卡前后确认没有创建广告计划、设置预算、启停投放、读取千川账户、写入项目或改变制作状态。
- [ ] 同时保留新旧两个批次并打乱时间线顺序，确认“最新批次投放交接”按方案生成时间选中最新批次，且始终按 B00、其他基线、A01/A02、其他版本显示。
- [ ] 最新批次全部就绪时确认版本数与 12 项检查总数准确；任一版本缺阶段、阈值、止损或单变量对照时，整批不得显示为已就绪，并准确列出对应测试编号。
- [ ] 取消不完整批次的复制确认，确认剪贴板和项目状态不变；确认后复制内容包含整批执行顺序和全部单条卡片。构造超过 40 个版本的异常批次，确认按钮禁用并要求拆分。
- [ ] 最新批次分别使用结果前置、证据前置、问题前置、价值表达、场景和受众变量，确认“灵感接力”只展示去重后的通用机制、启发问题和证据状态，默认保持折叠且不挤占实验下一步主操作。
- [ ] 在项目名、测试/批次编号、素材名、变量文本、完整脚本和指标中放入可识别测试字符串；复制脱敏灵感包后确认这些字符串和指标数值均不存在，输出要求至少改写三个创作维度。
- [ ] 口径提醒、样本不足、未达目标、高于父版本和达到目标分别只显示准确的非因果证据说明；复制前后不写项目、不保存分类或复制记录、不调用网络或远程模型。
- [ ] 逐张点击“复制共创挑战”，确认输出包含同机制新场景、反向机制和新证据三路发散，以及单变量假设、前三秒、受众场景、可核验证据、节奏/行动引导和停止条件。
- [ ] 篡改灵感卡的机制名、启发问题和原创提示自由文本，确认共创挑战只按已知机制/证据代码重新查固定文案，不带出篡改字符串；复制前后不写状态、不调用网络。
- [ ] 分别构造待制作、已上线待回填、数据异常、可决策、待重审、已完成和已搁置版本，确认每张卡默认只展开对应的制作、结果或决策层，完成与搁置态只保留摘要；手动切换层和快捷定位仍可用。
- [ ] 组合使用全部制作状态筛选、结果状态、批次和搜索；确认进度概览只统计当前项目完整台账，不受分页影响。
- [ ] 确认生成、复制和导出方案、导入或手动补录结果都不会自动修改制作状态；只有主动更改版本卡下拉框才写入。
- [ ] 导入包含已知、未知、重复和超限值的测试结果文件，确认只在预览并二次确认后写入已知编号。
- [ ] 在“快速回填单个版本”选择无结果和已有结果的版本：确认旧指标正确预载、百分比输入正确换算，预览前不写库，取消确认不变，最终确认才替换同编号结果。
- [ ] 手动回填缺少消耗、点击大于展示、转化大于点击、超过 100% 比例和 ROI / CTR / CVR 口径差异，确认与 CSV / TSV 使用相同的失败关闭或提醒规则。
- [ ] 导入点击大于展示、转化大于点击的数据，确认整次导入失败且旧结果不变；导入 ROI / CTR / CVR 口径差异数据，确认提醒在写入后仍显示。
- [ ] 分别筛选待回填、样本不足、达到目标、需要复核与口径提醒；导出 CSV / JSON 后确认包含当前项目全部版本且 CSV 公式文本不会执行。
- [ ] 构造至少 120 个版本，组合状态、批次和编号/父版本/变量搜索并逐页浏览；确认每页最多 20 张卡片、全部匹配项都可到达、无结果提示和页码准确。
- [ ] 在批次、状态和搜索条件会遮挡目标版本时点击“实验下一步”，确认条件被重置、目标测试编号进入精确搜索，并聚焦正确制作状态、决策或回填字段。
- [ ] 分别构造复盘失效、人工决策失效、指标口径提醒、待回填和最新批次额外变量风险；确认顶部“今日编导行动台”最多显示 3 项、优先级正确，并能精确打开对应表单或内容 Diff。
- [ ] 确认行动台不推断未标记版本的制作进度；人工标记后可准确显示相应状态，点击前后不新增长期队列、点击历史或自动写入。
- [ ] 分别从无结果、已有结果和有口径提醒的版本卡点击快捷按钮，确认准确载入对应编号和指标、旧值正确预载，且预览与二次确认前项目数据不变。
- [ ] 分别从无决策、有效决策和待重审版本卡打开人工表单；确认父版本快捷按钮只在有效“保留方案”或“继续测试”卡片显示，点击只选择父版本而不生成方案。
- [ ] 构造父子双方均达最低消耗的结果，检查 ROI、CTR、CVR、3 秒播放率和完播率的当前值/父值/差值；比例必须使用百分点，文案不得宣布胜负或因果。
- [ ] 分别让当前版本或父版本缺结果、低于最低消耗、存在指标口径提醒，确认对照只显示准确等待原因且不计算任何差值。
- [ ] 创建只改变声明变量的父子版本，展开内容 Diff，确认创意语义、测试上下文、生产资料分组以及父/当前文本正确，生产资料连带变化不误报为多个创意变量。
- [ ] 分别额外修改受众、场景、基线素材、保持不变项、观察指标和声明变量值，确认版本卡显示“需核对”；超长口播只展示有省略号的有界文本，方案原值不被修改。
- [ ] 在一个版本中保存继续测试、保留方案或停止测试的人工决策，检查主指标、护栏指标、理由、决策时间和台账导出；清除决策时版本与结果必须保留。
- [ ] 保存人工决策后分别重新导入结果、修改目标 ROI 和最低消耗，确认旧决策保留但显示“需重新确认”，重新保存后恢复有效。
- [ ] 分别构造失效决策、口径提醒、可决策、缺少 ROI、已上线待回填、未标记、制作中、已搁置和样本不足状态，确认“实验下一步”按既定优先级定位正确版本；只有人工标记为“已上线”且无结果的版本主动催回填，已有有效人工决策的状态不反复提示。
- [ ] 同时构造有效“保留方案”“继续测试”“停止测试”和待重审决策：确认只推荐最新的有效保留版本，无保留时才推荐继续版本，停止和待重审版本不推荐；点击建议只选中父版本，不自动生成方案。
- [ ] 从任一有效决策卡点击“设为下一轮父版本”，确认下拉框、帮助文字和焦点更新；随后生成新方案，时间线才新增正确父子关系。
- [ ] 导出 schema 3 全项目备份，在新的浏览器资料目录恢复后核对项目、版本、父版本、结果、制作状态和状态更新时间；确认 CSV / JSON 实验台账也包含制作状态字段。
- [ ] 未授权、无 Mask、清空 Mask、重置修复、PNG 透明通道和 JPEG 质量流程符合界面说明。
- [ ] 工作台与侧边栏的文件数量、单文件、总量和非媒体文件上限错误信息清晰。
- [ ] 确认侧边栏不再显示第二套转码文件、参数或队列；母版候选的视频按钮只打开独立工作台，并明确要求重新选择文件。
- [ ] 素材页默认只显示项目资产、母版匹配、图片修复和视频工作台入口；“维护与隐私”默认收起，展开后版本检查、备份、回退说明、隐私边界和三步引导入口均可到达。
- [ ] 在工作台展开“进阶输出设置”，分别检查均衡、高质量、小体积和指定码率；更改分辨率、帧率、音视频码率、采样率和后缀后，导出的标准化 MP4 任务参数正确，16 kHz 单声道 PCM 分析音频参数保持固定。
- [ ] 检查方案、结构分析和可拍草稿：日常复制、发送与交接动作直接可见，JSON / CSV / Markdown 位于“更多导出”且展开后均可正常下载。
- [ ] 在 1920px、1440px、1024px、720px、480px 和 280px 宽度检查工作台：页面无横向滚动，顶部导航、任务概览和唯一下一步按钮可用。
- [ ] 逐步验证顶部唯一主操作：选素材、补校验、生成任务、导出清单、导入结果、导入转写、分析、交接；每次点击只执行当前一步。
- [ ] 逐步核对顶部“成果去向”：未导出任务、手动正文和未保存分析显示“仅在本页”；文件导入正文显示“可重新导入”；导出分析、未确认草稿导出、草稿确认、交接失败/冲突和发送成功必须使用彼此不同且准确的状态。
- [ ] 确认导出未确认草稿不会显示为审核完成，勾选确认不会显示为已发送；只有成功入队才显示“已发送到编导台”，侧边栏仍需人工预览。
- [ ] 视频处理区“本机处理会做什么”默认收起，展开后标准化、提取音频和导入结果三项说明完整；生成任务、导出清单和导入结果仍无需展开说明即可操作。
- [ ] 首屏不预选入口，但“分析已有转写”位于推荐首选位；选择后主操作打开文本导入、辅助入口可聚焦粘贴区，视频与 FFmpeg 区块暂时收起且概览显示“本轮跳过”。
- [ ] 从已有转写切回“处理本地视频”，确认视频与处理区重新显示；来回切换时已有视频、任务、正文、分析和草稿保持不变。
- [ ] 在已有视频任务、转写正文和分析结果时来回切换入口，确认内容不被清空，提示列出保留内容，真实完成状态不倒退。
- [ ] 分别在空页面、已导出任务、未导出任务、可从导入文件重建的正文、手动粘贴正文、未保存分析和草稿状态刷新或关闭：只有确有页面成果丢失风险时出现浏览器原生提醒，确认离开后仍不会自动恢复或写入长期存储。
- [ ] 分别在仅选入口、已填路径、已选视频、任务待导出、转写任务待导出、已有正文、分析待导出、交接失败和交接完成状态点击“开始新一轮 / 重置本轮”；确认按钮始终可见且保持辅助视觉层级。
- [ ] 重置确认文案准确列出尚未导出的处理/转写任务和未导出或未成功发送的分析；点击取消后逐项确认文件、字段、正文、结果、进度和错误完全不变。
- [ ] 确认重置后所有文件 input、入口单选、路径和授权参数、任务区、正文、分析结果、状态、进度与错误回到初始态；原文件、侧边栏工作区、`chrome.storage.local` 和既有 session 收件箱结果保持不变，焦点回到首个入口。
- [ ] 处理失败、结果导入失败、交接失败与收件箱冲突时，顶部状态和重试入口准确，原任务、转写或分析结果不丢失。
- [ ] 快速连续选择两个转写文件时只采用最后一次；读取大文件期间重置本轮后旧内容不重新出现；重建处理任务期间导入旧结果不会覆盖新任务；重复点击交接不会让旧完成回写新页面状态。
- [ ] 在浏览器 200% 缩放下检查长路径、PowerShell 命令、任务队列与分析结果不遮挡按钮。
- [ ] 工作台导出交接包后，侧边栏先预览、仅确认填入空白字段；清除未应用预览可重导，成功应用后同会话拒绝重复应用。
- [ ] 工作台点击“发送并打开编导台”：空槽入队并显示预览；同一分析重复发送幂等；已有不同结果时不覆盖，先提示处理旧结果。
- [ ] 已有不同 JSON 或会话预览时，新 session 结果只显示“另有待确认结果”；主动切换后才替换预览，应用或明确清除后才消费。
- [ ] 模拟 `sidePanel.open()` 不可用或拒绝：交接仍在 session 收件箱，工作台明确提示点击扩展图标；备用 JSON 仍可导出和导入。
- [ ] 构造损坏、未知字段、超限和超过两小时的 session envelope，确认失败关闭、清理且创作任务不变。
- [ ] 用错误 schema、错误来源、未知字段、超长值、本机路径、活动内容和篡改 ID 的交接包逐项确认失败关闭，原任务不变。
- [ ] 分别导入 SRT、VTT、TXT 与 MD，确认分析片段显示正确的时间/段落来源；编辑正文后旧映射立即失效并提示。
- [ ] 检查 Transcript v2 摘要的来源、格式、分段、时长、解析器和指纹；导入时间重叠 SRT 后只提示人工核对且不静默改写时间。
- [ ] 未选择改进建议时不能生成草稿；选择建议后只生成一个主要测试变量，修改草稿后旧确认立即取消。
- [ ] 检查草稿的测试编号、父版本、匿名来源分析、11 个可编辑字段、证据引用、JSON 与 Markdown 导出。
- [ ] 未明确确认时不能发送或导出 V2 交接；确认后同包幂等、异包冲突、过期与 JSON 备用流程保持正确。
- [ ] 侧边栏完整只读显示 V2 草稿，确认它不覆盖已有创作任务字段或 `creativePlan`；V1 交接继续兼容。
- [ ] 页内下一步、今日编导行动台、标签分页与分析结果跳转后，实际焦点目标处于可见区域；开启“减少动态效果”时不平滑滚动。
- [ ] 所有清空/重置确认在“取消”后保持原状态，在“确认”后仅清理当前会话目标。
- [ ] 默认不访问 GitHub；主动检查时才申请可选权限，拒绝权限后不发起版本请求。
- [ ] 确认来源 URL 的“打开原页”显示为备注旁的弱操作；不填写时只提示校验错误，填写合法链接后仅在用户点击时执行普通新标签页导航。
- [ ] DevTools Console 无未处理 Promise 拒绝、CSP 报错或运行时异常。

## 安全与产品边界人工复核

- [ ] 没有页面读取、内容脚本、平台请求拦截、链接视频解析/下载或远程转写。
- [ ] 没有暗水印、隐藏指纹、版权/溯源标识检测、定位、移除或规避功能。
- [ ] 图片修复只修改用户人工 Mask 与向内羽化范围，原图不覆盖。
- [ ] PowerShell 任务仅接受固定参数白名单；输入、输出和可执行路径仍经过现有路径校验。
- [ ] 导出的任务清单可能包含本机路径或来源备注，文档已提醒只在可信设备保存。
- [ ] 分析交接包不包含原始转写全文、来源文件名、视频信息、本机路径或未知私有字段；V2 草稿只包含固定白名单。
- [ ] 投放交接卡只由用户点击写入剪贴板；缺项检查和单变量对照不持久化、不联网、不跨批次猜测，也不执行任何千川平台操作。

## 打包、安装与回退

- [ ] 从干净检出生成源码包，排除 `.git`、`node_modules`、本机缓存、测试输出和私钥。
- [ ] 解压发布包后再次执行 `npm test` 与 `npm run check`。
- [ ] 更新前导出工作区备份，并复制当前已验证扩展目录。
- [ ] 替换源码后在 `chrome://extensions` 点击“重新加载”，再执行核心人工验收。
- [ ] 回退演练：恢复旧目录、重新加载、按需导入工作区备份，确认旧版本可打开。
- [ ] GitHub Release 说明包含版本、校验值、变更、安全边界、已知限制和回退步骤。

在所有自动项通过、Chrome 人工项完成之前，只能称为“V1.4.0 源码发布候选”，不能宣称已完成浏览器兼容验收或正式发布。
