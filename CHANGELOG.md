# 变更记录

本项目采用语义化版本号。此文件记录源码版本的用户可见变化；GitHub Release 是否已经发布以仓库发布页为准。

## [1.4.0] - 2026-09-01

### 新增

- 新增每个测试版本的人工制作状态：未标记、待拍、拍摄中、剪辑中、待投放、已上线和已搁置，并记录编导主动更新时间。
- 新增制作进度概览和制作状态筛选；版本卡可更新或清除状态，失败时恢复原选择，不显示未写入的假进度。
- 新增 `src/production-status.js` 与对应测试，集中处理固定状态、时间校验、标签和项目级进度汇总。

### 改进

- “实验下一步”和顶部行动台只对人工标记为“已上线”且无结果的版本主动提示回填；未上线或未标记版本改为提示维护制作进度。
- 所有行动入口使用测试编号精确搜索并定位目标版本，避免版本超过一页时聚焦失败。
- 版本卡、快速回填选择器和父版本选择器区分制作进度与投放结果；未上线且无结果时显示“补录结果”，不再伪装成已上线待回填。
- 项目备份继续兼容 schema 3，人工制作状态作为可选版本字段；旧 V1.3 记录安全显示为“未标记”。实验台账 schema 升级为 3，增加制作状态和更新时间。
- 独立工作台把“分析已有转写”提升为推荐快速路径；选中后暂时收起不需要的视频与 FFmpeg 区块，并把对应步骤显示为“本轮跳过”，切回视频入口即可恢复。
- 工作台面板和侧栏导航同步显示当前、完成、提醒与跳过状态，减少长页面中丢失当前位置的问题。
- 转写导入、本机执行结果导入和会话交接增加最新操作保护；换文件、重建任务或重置页面后，较早完成的异步任务不会覆盖新状态。
- 侧边栏的重复转码队列已合并进独立工作台；母版匹配只提供安全入口，不再维护第二份文件选择、参数、任务和结果状态。
- 独立工作台补齐编码预设、分辨率、帧率、视频码率、音频码率、采样率和输出后缀；分析音频仍固定为本地 16 kHz 单声道 PCM。
- 方案、分析和草稿的 JSON / CSV / Markdown 等低频格式收进“更多导出”，减少日常界面的并列按钮。

### 安全与兼容

- 生成、复制或导出方案不会自动改变制作状态，导入投放结果也不会反向设置“已上线”；所有制作状态只能由编导主动选择。
- 制作状态保存在当前浏览器 IndexedDB，不新增权限、网络请求、平台页面读取或远程服务。
- 异步操作保护只保存短生命周期内存令牌，不持久化文件、文本、路径、操作历史或用户行为数据。
- 视频处理统一采用工作台的 40 个文件、单文件 10 GB、合计 40 GB 失败关闭上限；原片不覆盖，进阶参数不写入长期存储。
- 本版本继续不包含链接视频解析、无水印提取、平台隐藏标识处理或自动投放能力。

## [1.3.0] - 2026-08-24

### 新增

- 新增 Transcript v2 统一转写契约：TXT、MD、SRT、VTT、手动粘贴和明确标记的本机 whisper.cpp 结果可共享来源类型、格式、语言、分段、时长、解析器版本、本地内容指纹与解析提醒。
- 工作台新增统一转写摘要，显示来源、分段、时长、解析器和本地指纹；字幕时间重叠会保留原时间并提示人工核对。
- 新增每个测试版本的人工决策卡。编导可记录继续测试、保留方案或停止测试，选择主指标与护栏指标并填写判断理由。
- 新增 `src/experiment-decision.js` 与对应测试，把人工决策校验、数据健康、证据快照和失效判断从界面交互中分离。
- 新增单版本快速回填：可直接填写消耗、ROI、成交金额、漏斗与播放指标；已有结果先预载，仍需预览和二次确认才能替换。
- 新增“实验下一步”行动卡和 `src/experiment-actions.js`，优先提示失效决策、口径提醒、待决策或待回填版本，并定位到对应表单。
- 新增下一轮父版本建议和版本卡快捷入口：只使用证据仍有效的“保留方案”或“继续测试”人工结论，用户点击后才选中。
- 新增实验版本批次筛选、测试编号/父版本/变量搜索和分页；`src/experiment-view.js` 保证单项目 500 个版本都可访问。
- 新增版本卡上下文操作：可直接回填、核对或更新当前编号的结果，记录、查看或重新确认人工决策，并从有效人工结论选择父版本。
- 新增父子版本折叠对照和 `src/experiment-comparison.js`，并列展示共同存在的 ROI、CTR、CVR、3 秒播放率与完播率。
- 新增父子可拍内容 Diff 和 `src/creative-version-diff.js`，按创意语义、测试上下文和生产资料分组展示父版本/当前版本。
- 新增顶部“今日编导行动台”和 `src/director-desk.js`：把可靠核心流程、实验待办和最新批次单变量风险合并为最多 3 项，并可直达回填、决策、父版本或内容 Diff。

### 改进

- 旧 V1 转写对象在读取时升级为 Transcript v2，不覆盖原对象；正文编辑后仍立即作废旧时间/段落映射。
- 人工决策绑定当前结果、描述性状态、目标 ROI、最低消耗和版本内容的短证据指纹；任一证据变化后旧结论保留审计但标记为“需重新确认”。
- 实验时间线增加数据健康提示、人工决策筛选和待重审数量；CSV / JSON 台账增加决策状态、结论、主/护栏指标、理由和决策时间。
- 手动单版本回填与 CSV / TSV 共用数值、比例、不可能漏斗和指标口径校验；结果写入后同步更新项目的最近修改时间。
- 父版本下拉项显示人工决策状态；建议优先使用“保留方案”，无保留结论时才使用“继续测试”，并在选中后明确提示尚未生成新方案。
- 移除实验时间线静默只显示前 100 条的限制；每页只渲染 20 张卡片，查找和翻页不再反复重建完整父版本与手动回填选项。
- “实验下一步”定位前会清除可能遮挡目标的批次与搜索条件并回到第一页；筛选和分页仍不影响完整台账导出。
- 卡片回填与“实验下一步”共用同一个精确载入函数；已有指标会预载，按钮只移动焦点和准备表单，不绕过预览、二次确认或人工保存。
- 父子对照要求双方均达到各自最低消耗且没有指标口径提醒；比例变化使用百分点，缺数据时只展示等待原因，不生成推断。
- 单变量检查识别钩子、核心主张、拍摄场景和目标受众；允许生产资料连带改写，但额外语义变化、基线/固定项/观察口径变化及声明值不一致会提示人工核对。
- 单字段 Diff 展示限制为 800 个字符，使用纯文本节点和本地折叠面板，不执行或持久化内容。
- 行动台按数据失效与口径风险、最新批次内容风险、待决策/待回填和常规流程排序；只呈现本地可证实状态，不虚构拍摄或上线进度。
- 项目集合备份继续使用兼容 schema 3；版本记录的人工决策为可选字段，旧项目、旧备份和无决策版本可直接读取。

### 安全与兼容

- 人工决策只能由用户提交，系统不自动决定继续、保留或停止，也不把描述性差异宣称为因果。
- 行动卡只排序当前项目中的人工待办，不自动回填结果、覆盖旧数据或执行任何平台操作；有效人工决策会阻止同一状态被反复提示。
- “停止测试”和待重审决策不会被推荐为下一轮起点；父版本建议不是统计显著性、因果关系或胜负判定。
- 父子指标差值只在页面内由已保存结果计算，不写回结果、不改变人工决策，也不把相关差异描述为因果或统计显著性。
- 内容 Diff 只是对已保存白名单字段的人工核对辅助，不自动修改方案、阻止导出或认定测试有效性。
- 行动台队列完全在页面内派生，不新增存储键或权限；每个动作仍需用户点击，不会自动写结果、保存决策、生成方案或执行平台操作。
- Transcript 与决策指纹均为本地一致性标记，不是版权、身份或统计显著性证明；不包含原始报表行、媒体内容、账号数据或本机路径。
- 本版本不新增第三方依赖、浏览器权限、远程代码、内容脚本、平台页面读取、云端转写、链接媒体解析、无水印提取或隐藏标识处理能力。

## [1.2.0] - 2026-08-23

### 新增

- 新增当前浏览器内的多项目工作区：可新建、重命名和切换最多 20 个项目；旧单工作区首次启动时自动迁移为第一个项目。
- 新增测试版本链。每次生成方案使用带毫秒时间的唯一批次编号，编导可显式选择父版本或新测试起点；单项目最多 500 个版本。
- 新增测试结果 CSV / TSV 回填，按测试编号匹配消耗、ROI、成交金额、展示、点击、转化、CTR、CVR、3 秒播放率与完播率；未知编号先预览且不写入。
- 新增 `src/project-model.js`、`src/project-store.js` 与 `src/experiment-results.js`，把数据边界、IndexedDB 持久化和结果解析从侧边栏交互中分离。
- 新增实验台账状态汇总和筛选，可查看待回填、样本不足、达到目标、需要复核与口径提醒，并把当前项目完整台账导出为 CSV / JSON。

### 改进

- 版本状态按最低测试消耗、目标 ROI 和父版本结果生成“样本不足、达到目标、高于/低于父版本”等描述性状态；明确不把差异当成因果证明。
- 工作区备份升级为 schema 3，可同时导出和恢复项目集合、版本与结果；仍兼容 V0.5 schema 1 与 V0.6–V1.1 schema 2。
- 项目切换采用待切换标记和重载后恢复，避免把当前项目快照误写到目标项目；存在页面内临时素材任务时先提示确认。
- 同主题、同变量的重复方案不再复用测试编号，解决旧版本记录被覆盖的问题。
- 结果导入新增漏斗一致性校验；ROI、CTR、CVR 与基础指标差异较大时显示并持久保留固定口径提醒，避免确认后失去数据质量上下文。

### 安全与兼容

- 多项目、版本和结果只保存在扩展本地 IndexedDB；当前工作区继续保存在 `chrome.storage.local`，IndexedDB 不可用时安全降级为原有单项目模式。
- 测试结果最大 2 MB / 1,000 行，工作区备份最大 12 MB；项目、版本、结果、父版本和引用关系均失败关闭校验。
- schema 3 备份拒绝循环父版本；若项目集合写入后浏览器存储更新失败，会尝试回滚到导入前项目集合和当前工作区，并在回滚不完整时明确报错。
- 实验台账导出不受页面筛选影响，不包含原始报表、素材、完整拍摄脚本或本机路径；CSV 文本字段继续执行公式安全处理。
- 运行时新增本地固定依赖 `idb 8.0.3`（ISC，约 8 KB 未压缩），源码与许可证随仓库分发，不从 CDN 加载。
- Manifest 权限和 CSP 不变；不新增页面读取、平台请求、Cookie/Token、自动投放结果同步、链接媒体解析、无水印提取或隐藏标识处理能力。

## [1.1.0] - 2026-08-08

### 新增

- 新增独立 `timed-transcript` 模块：SRT / VTT 保留 cue 起止时间、标签和正文，TXT / MD 使用空时间段落；手工编辑正文后旧映射明确失效。
- 结构分析分段新增时间或段落来源，并提供七类可选改进建议；用户必须先选择一项，才可进入下一版草稿。
- 新增独立 `creative-revision` 与 `revision-id` 模块，生成带本地唯一测试编号、父版本和匿名来源分析编号的完整可编辑拍摄草稿。
- 草稿覆盖问题摘要、测试假设、唯一变量、保持不变项、前三秒钩子、口播、分镜、补拍、字幕、剪辑节奏、成功指标和停止条件。

### 改进

- 工作台闭环升级为“分析 → 选择一项改进 → 生成单变量草稿 → 编辑 → 明确确认 → V2 交接”。任何编辑都会取消旧确认和交接缓存。
- 顶部唯一主操作会依真实状态聚焦建议、生成草稿、要求确认或执行发送；重置提示新增尚未导出或交接草稿风险。
- 侧边栏新增 V2 只读草稿预览，显示测试关系、证据来源和全部白名单字段；旧四类建议的填空流程保持兼容。
- 工作台宽屏、400px 与 280px 布局补充草稿卡、长文本与操作区响应式样式。

### 安全与兼容

- 旧 V1 分析交接继续校验和导入；V2 只在附带严格验证的白名单草稿时启用，仍沿用同一 session 单槽、幂等、冲突与两小时 TTL。
- V2 草稿最大 48 KB，交接包仍最大 128 KB；未知字段、本机路径、活动内容、非法时间、跨分析引用和多主要变量失败关闭。
- 侧边栏不把 V2 草稿写入 `creativePlan`，不自动覆盖任何任务或方案；只有旧四类建议仍可由用户确认填入空白字段。
- 本版本不新增依赖、存储键、权限、远程代码、平台页面读取、Cookie/Token、链接媒体解析、无水印提取或隐藏标识处理能力；工作区备份 schema 保持不变。

## [1.0.7] - 2026-08-08

### 新增

- 独立工作台首屏新增常驻、低强调的“开始新一轮 / 重置本轮”入口，在视频、任务、转写、分析、交接完成或错误等任意页面状态下均可使用。
- 重置前由纯状态模型列出当前将被清空的页面内容，并特别标出尚未导出的处理任务、转写任务，以及尚未导出或成功发送的分析结果。

### 改进

- 取消确认时不修改任何页面状态；确认后一次性复位文件选择、路径与授权参数、处理和转写任务、正文、分析、交接状态、隐藏结果区、进度、入口模式及错误提示，并立即回到双入口初始态。
- 重置按钮保持辅助视觉层级，顶部动态主操作仍是全页唯一主操作；原生确认框、可见说明和重置后焦点回到入口单选项，补齐键盘与读屏路径。
- 窄屏入口、提示和长文本增加可断行保护，保持 400px 与高缩放下的单列布局。

### 安全与兼容

- “开始新一轮”只销毁当前工作台页面内存，不调用任何存储删除 API，不删除或覆盖本地原文件、已下载导出文件、`chrome.storage.local` 工作区、侧边栏数据或已发送的会话收件箱结果。
- 本版本不新增依赖、存储键、权限或远程能力；不解析或下载抖音链接，不读取 Cookie/Token，不提取无水印地址，也不处理平台隐藏标识。

## [1.0.6] - 2026-08-08

### 新增

- 工作台首屏新增“处理本地视频 / 分析已有转写”两种轻量入口，初始状态不再默认要求所有用户先选择视频。
- “分析已有转写”入口的顶部唯一主操作可直接打开本地文本文件选择；同时提供一个非主视觉的“直接粘贴转写”入口，将键盘焦点送到正文区域。

### 改进

- 两条路径继续复用同一个顶部主操作和四步状态模型：视频路径保留授权、任务、结果、转写、分析与交接；转写路径不要求先生成视频任务。
- 入口选择只在当前页面内存中生效。切换入口不会清空已选视频、任务清单、转写正文或分析结果，并会明确列出当前保留内容。
- 已有转写或分析结果始终优先按真实完成状态继续，避免切换入口后把已完成工作隐藏或误标为未开始。

### 安全与兼容

- 入口选择、阶段和进度不新增 `chrome.storage` 键，也不进入工作区备份；刷新或关闭后仍不会伪装恢复媒体会话。
- Manifest 权限、CSP、本地任务校验、会话交接和平台边界保持不变；不解析或下载抖音链接，不读取 Cookie/Token，不处理平台隐藏标识。

## [1.0.5] - 2026-08-08

### 改进

- 独立工作台顶部从“只滚动定位”的下一步导航升级为唯一主操作：根据真实页面状态依次选择素材、补齐校验、生成任务、导出清单、导入结果、导入转写、分析结构并发送编导台。
- 新增统一阶段标签和本轮工作流完成度，集中显示当前状态、下一步原因、可恢复错误与交接结果；分区内原有按钮继续保留为辅助入口，但不再与顶部主操作争夺视觉层级。
- 处理清单导出或命令复制后，主操作明确推进到“导入本机结果”；处理异常、收件箱冲突和发送失败都有稳定重试或降级路径，不会丢失当前页面结果。
- 完成交接后主操作切换为“打开编导台查看结果”，避免重复排队；收件箱已有不同结果时只打开侧边栏处理冲突，不覆盖已有内容。

### 安全与兼容

- 顶部主操作仍只响应用户点击，不绕过素材授权、文件类型、大小、路径、任务结果或交接包校验；不会静默启动 FFmpeg、下载平台视频或自动提交表单。
- 明确说明视频选择、处理队列和转写正文只在当前页面保留；本版本不新增持久化键，不把无法恢复的媒体会话伪装成“继续任务”。
- Manifest 权限、CSP、备份 schema 和本地处理边界保持不变；未新增平台页面读取、私有接口、Cookie/Token、无水印提取或隐藏标识处理。

## [1.0.4] - 2026-08-07

### 新增

- 工作台结构分析结果新增“发送并打开编导台”，把受限交接放入当前浏览器的 `chrome.storage.session` 后尝试调用 `sidePanel.open()`。
- 侧边栏新增单槽会话收件箱状态、主动切换、明确清除及 `chrome.storage.onChanged` 实时响应；初始化会在工作区恢复完成后读取待确认结果。
- 新增 `src/analysis-handoff-inbox.js`，集中处理固定 envelope、两小时 TTL、大小与来源复核、幂等、冲突、坏数据和过期清理。

### 改进

- 同一次结构分析复用同一 handoff ID，重复发送不会误报为不同结果；正文变化或重新分析后才生成新的交接。
- 已有不同 JSON 或会话预览时不自动覆盖，只提示“另有待确认结果”，由用户主动切换；成功应用仍只填选中的空白字段。
- 保留“导出 JSON 交接包”完整备用流程。Chrome 114/115、会话存储不可用或侧边栏打开失败时提供清晰降级提示，不伪装成功。

### 安全与兼容

- 会话 envelope 与内部固定交接包在发送前、写入后和读取时重复校验；不包含原始转写、文件名、视频、本机路径、账号或平台数据。
- 会话结果不进入长期工作区或备份；应用成功或用户明确清除后消费，过期或损坏结果失败关闭并清理。
- Manifest 权限保持为 `sidePanel`、`storage`，未新增远程处理、页面读取、平台下载、暗水印或溯源标识处理能力。

## [1.0.3] - 2026-08-06

### 新增

- 独立工作台新增“交接到编导台”，导出固定 schema、最大 128 KB 的本地 JSON 交接包。
- 侧边栏创作任务新增交接包导入、四类短建议预览、逐项勾选和用户确认填空流程。
- 新增 `src/analysis-handoff.js`，集中处理来源、方法、字段、大小、长度、路径、活动内容、篡改标识与重复应用校验。

### 改进

- 交接包只带结构摘要、覆盖项、缺项建议以及目标受众、受众问题、核心主张、可用证据四类短建议，不带原始转写全文、来源文件名、视频信息或本机路径。
- 已有创作任务内容始终跳过；校验成功只生成预览，实际填入至少一个字段后才标记为已应用。清除未应用预览后仍可重新导入同一包。
- 页内下一步、最近任务、分页、分析结果与修复状态统一采用“聚焦后将实际目标滚入视口”，尊重减少动态效果设置，不再用 `preventScroll` 把焦点留在屏外。

### 安全与兼容

- 分析交接使用本地确定性规则，明确不是投放效果预测；不新增远程接口、权限、存储键或备份 schema。
- 错误 schema、非工作台来源、未知字段、超长值、疑似本机路径、活动内容、篡改标识及已成功应用的重复包均失败关闭。
- 保持 Manifest 权限为 `sidePanel`、`storage` 及仅用于版本检查的可选 GitHub API 主机权限。

## [1.0.2] - 2026-08-06

### 改进

- 将“素材处理与分析工作台”提升到侧边栏标题下方、步骤导航之前，打开扩展首屏即可看到唯一主入口。
- STEP 04 删除重复主入口，只在项目空状态保留“处理本地素材”的语境化小入口。
- 侧边栏明确定位为“编导决策台”，独立页面继续定位为深度“素材处理与分析工作台”。
- 新增紧凑的“最近任务”继续卡，根据现有创作任务、复盘、方案、失效与导出状态定位到可靠的下一步。
- 最近任务空状态明确说明：独立工作台的视频、转写和处理队列只存在于其页面会话，当前版本不会伪装成可恢复历史。
- 280–400px 窄侧边栏下，首屏入口与最近任务保持紧凑排布，避免把步骤导航和当前任务过度下推。

### 安全与兼容

- 最近任务完全由已有本地工作区状态派生，不新增存储键，不改变备份 schema。
- 不新增权限、远程处理、页面读取、平台链接下载或平台隐藏标识处理能力。

## [1.0.1] - 2026-08-06

### 改进

- 将独立页面统一命名为“素材处理与分析工作台”，与侧边栏编导决策台区分职责。
- 桌面端侧栏加宽至 248px，主工作区最大宽度提高至 1400px，并在超宽屏可用区域内居中。
- 新增素材、处理、转写、分析四步任务概览与唯一“下一步”页内导航；按钮只滚动和聚焦，不自动提交或运行处理。
- 1100px 以下改为顶部横向导航，720px 以下表单、按钮、工作流卡、覆盖项和结果头改为单列。
- 宽屏下素材配置使用“素材选择 + 三列参数”，任务队列保持全宽，结构分段与建议可双列展示。
- 移除页面 920px 最小宽度限制，并补充长路径、命令及 280–480px 窄窗口的安全换行。

### 安全与兼容

- 不改变存储模型、备份 schema、Manifest 权限、CSP 或本地处理边界。
- 不新增远程服务、持久化任务、平台页面读取、链接媒体下载或平台标识处理能力。

## [1.0.0] - 2026-08-05

V1.0.0 是首个面向公开源码发布的候选版本，保留 V0.5 / V0.6 本地数据兼容，不提高备份 schema。

### 新增

- 首次使用三步引导，可收起并从“隐私与产品边界”重新打开。
- 逐键工作区恢复与可见恢复面板；异常键必须由用户显式确认后清理。
- 非媒体导入统一限制：报表 8 MB、备份 2 MB、执行结果 2 MB、转写文本 4 MB。
- CSV 结构保护：最多 50,000 行、200 列，并拒绝未闭合引号、空白或重复表头。
- 下一版任务 CSV 公式安全转义，避免用户文本被电子表格当作公式执行。
- `CHANGELOG.md`、发布检查清单和 V1 发布级静态测试。

### 改进

- 初始化拆分为读取、逐键恢复、兼容迁移、隔离渲染和 UI 收尾；单条损坏记录不会让整个侧边栏无法打开。
- 报表、备份、执行结果或转写解析失败时保留当前成功状态。
- 复盘或方案保存失败时保留当前会话结果，允许用户先复制或导出。
- 工作区备份直接从当前已校验状态生成，不依赖重新读取可能损坏的原始存储。
- 清空 Mask、重置修复、清空转码选择和服务台文件选择增加明确确认。
- 后台侧边栏配置、延迟方案保存和更新设置写入改为失败可见。

### 安全

- Manifest V3 显式 CSP：`script-src 'self'; object-src 'none';`。
- 权限保持为 `sidePanel`、`storage`，仅把固定 GitHub API 保留为可选主机权限。
- 不新增内容脚本、页面读取、下载、网络拦截、Native Messaging 或远程运行时代码。
- 保持平台标识边界：不检测、定位、破解、移除暗水印、隐藏指纹、版权标记或溯源标识。

### 兼容

- 继续读取 V0.5 schema 1 备份并映射旧 `productBrief`。
- 继续读取 V0.6 schema 2 备份；备份 schema 保持 2。
- V0.6.1 及更早、缺少完整依赖指纹的方案恢复后要求重新生成。

### 已知限制

- 只保存最近一个工作项目；多项目与投放结果自动回填尚未实现。
- 浏览器不会直接启动 FFmpeg 或 whisper.cpp，仍需用户导出并在本机运行受限任务。
- 图片局部修复只支持自有或已授权静态图片的人工 Mask 普通瑕疵修复，不支持视频修复或任何平台标识处理。
- Chrome“加载已解压”模式不会静默更新；用户必须备份、替换源码并手动重新加载。
