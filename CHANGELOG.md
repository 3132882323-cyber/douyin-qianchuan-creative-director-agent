# 变更记录

本项目采用语义化版本号。此文件记录源码版本的用户可见变化；GitHub Release 是否已经发布以仓库发布页为准。

## [1.0.0] - 2026-08-05

V1.0.0 是首个面向公开源码发布的候选版本，保留 V0.5 / V0.6 本地数据兼容，不提高备份 schema。

### 新增

- 首次使用三步引导，可收起并从“隐私与产品边界”重新打开。
- 逐键工作区恢复与可见恢复面板；异常键必须由用户显式确认后清理。
- 非媒体导入统一限制：报表 8 MB、备份 2 MB、执行结果 2 MB、转写文本 4 MB。
- CSV 结构保护：最多 50,000 行、200 列，并拒绝未闭合引号、空白或重复表头。
- 下一版任务 CSV 公式安全转义，避免用户文本被电子表格当作公式执行。
- `CHANGELOG.md`、发布检查清单和 V1 发布级静态测试。

### 改进

- 初始化拆分为读取、逐键恢复、兼容迁移、隔离渲染和 UI 收尾；单条损坏记录不会让整个侧边栏无法打开。
- 报表、备份、执行结果或转写解析失败时保留当前成功状态。
- 复盘或方案保存失败时保留当前会话结果，允许用户先复制或导出。
- 工作区备份直接从当前已校验状态生成，不依赖重新读取可能损坏的原始存储。
- 清空 Mask、重置修复、清空转码选择和服务台文件选择增加明确确认。
- 后台侧边栏配置、延迟方案保存和更新设置写入改为失败可见。

### 安全

- Manifest V3 显式 CSP：`script-src 'self'; object-src 'none';`。
- 权限保持为 `sidePanel`、`storage`，仅把固定 GitHub API 保留为可选主机权限。
- 不新增内容脚本、页面读取、下载、网络拦截、Native Messaging 或远程运行时代码。
- 保持平台标识边界：不检测、定位、破解、移除暗水印、隐藏指纹、版权标记或溯源标识。

### 兼容

- 继续读取 V0.5 schema 1 备份并映射旧 `productBrief`。
- 继续读取 V0.6 schema 2 备份；备份 schema 保持 2。
- V0.6.1 及更早、缺少完整依赖指纹的方案恢复后要求重新生成。

### 已知限制

- 只保存最近一个工作项目；多项目与投放结果自动回填尚未实现。
- 浏览器不会直接启动 FFmpeg 或 whisper.cpp，仍需用户导出并在本机运行受限任务。
- 图片局部修复只支持自有或已授权静态图片的人工 Mask 普通瑕疵修复，不支持视频修复或任何平台标识处理。
- Chrome“加载已解压”模式不会静默更新；用户必须备份、替换源码并手动重新加载。
