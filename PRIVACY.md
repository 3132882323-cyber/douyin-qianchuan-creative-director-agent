# 隐私说明

## 本地处理

- 可选创作任务、复盘摘要、创意策略和用户编辑后的生产资料保存在当前浏览器的 `chrome.storage.local`。
- 首次三步引导是否已收起只保存为一个本地布尔设置；用户可以随时从“隐私与产品边界”重新打开引导。
- 自 V1.0.0 起，从 V0.5 升级时旧版创作资料会一次性映射为新的创作任务；V0.6 的当前结构可直接恢复。旧原值仅暂存在当前浏览器的本地迁移归档中，不参与新方案生成，也不进入新备份；只有迁移结果成功写回后才清理旧存储键。
- 工作区启动采用逐键恢复。损坏记录会被隔离并在界面列出具体存储键，不会导致整个侧边栏空白；扩展不会自动删除这些键，只有用户显式点击并再次确认后才清理。
- 原始 CSV、视频、图片和母版文件只在用户主动选择后于本地浏览器内读取与计算，不上传到服务器。
- 扩展不会自动浏览本地文件，也不会把原始素材内容写入扩展存储。
- 自有母版转码队列只保留在当前侧边栏会话中；扩展不会把视频内容放入 `chrome.storage.local`。
- 图片局部修复的原图、Mask 与结果只保留在当前侧边栏页面内；扩展不会覆盖原图，只有用户点击导出后才创建新文件。
- 素材处理与分析工作台中的视频列表、处理清单、转写文本和结构分析只保留在当前工作台页面内，除非用户主动导出任务或结果文件。
- 工作台分析只在用户点击“发送并打开编导台”后写入当前浏览器的 `chrome.storage.session`，或在用户点击“导出 JSON 交接包”后创建本地文件。固定结构只包含摘要、结构覆盖、缺项建议和四类最多 800 字的短建议，不包含原始转写全文、来源文件名、视频、账号数据或本机路径。
- V1.0.7 的“处理本地视频 / 分析已有转写”入口、顶部唯一主操作、阶段、完成度和集中错误都只根据当前页面内存计算，不写入新存储键。入口切换不会清空已选视频、处理队列、转写正文或分析结果；这些内容仍不持久化，刷新或关闭页面后不会被描述为可恢复任务。
- 用户点击“开始新一轮 / 重置本轮”时，扩展先列出将被清空的当前页面内容，并标明尚未导出的任务或分析；取消确认不会修改状态。确认后只复位当前工作台的文件引用、路径与授权参数、任务、转写正文、分析、本页交接状态和界面提示，不调用存储删除 API，不删除本地原文件、已下载文件、侧边栏数据、`chrome.storage.local` 工作区或已发送到会话收件箱的结果。
- 会话交接使用固定单槽 envelope，并在两小时后过期；相同结果重复发送保持幂等，不同待处理结果不会静默覆盖。坏数据和过期数据失败关闭并清理，应用成功或用户明确清除后才消费该槽。会话交接不进入 `chrome.storage.local`、工作区备份或长期任务历史。
- 顶部“最近任务”只根据当前本地工作区中的创作任务、复盘摘要、下一版方案和导出状态计算继续位置，不新增任务历史或访问时间记录，也不会声称可以恢复独立工作台的页面会话。
- 下一版任务会保存一个不可逆的短依赖指纹，用于判断字段映射、复盘排名、标签、ROI、创作任务和生成参数是否变化。它不是文件指纹，也不包含原始 CSV 或素材内容；旧方案缺少该字段时会安全要求重新生成。
- 用户可以通过移除扩展或在浏览器扩展设置中清除数据，删除本地工作区内容。

## 不会进行的操作

- 不读取或保存千川账号密码。
- 不拦截千川或抖音页面的网络请求。
- 不向网页注入脚本，不使用远程托管代码。
- 扩展页使用 Manifest V3 CSP `script-src 'self'; object-src 'none';`，不允许运行远程脚本或对象插件。
- 不向开发者、第三方分析服务或广告平台发送使用数据。
- 不通过抖音链接解析或下载视频，不调用云端转写，不使用浏览器可能联网的语音识别接口。

## 版本检查

- 默认不会访问 GitHub。用户点击“检查新版本”或明确开启“打开侧边栏时自动检查”后，浏览器才会请求本项目的公开 GitHub Release 元数据。
- 首次检查需要用户授予 `https://api.github.com/*` 可选主机权限；未授权时不会发起网络请求。
- 请求不包含创作任务、投放报表、脚本、素材、文件名、Cookie、Token 或扩展签名私钥。
- GitHub 会像处理普通网络访问一样接收 IP 地址和标准请求信息；具体处理方式受 GitHub 隐私政策约束。
- Release 返回内容只用于比较版本号并提供项目发布页地址，不会作为脚本执行，也不会自动下载或安装文件。
- 自动检查最多每天一次，并且只在侧边栏被打开时运行。用户可以随时关闭。

工作区备份只由用户主动导出到本地，包含经过结构白名单筛选的创作任务、复盘摘要、拍摄方案和更新设置；未知顶层或嵌套字段不会进入新备份。备份不包含原始 CSV、视频、图片、账号凭据、私钥、会话交接、工作台入口选择或旧版本地迁移归档。导入备份前会再次要求用户确认覆盖当前工作区。V1.0.7 继续读取 V0.5 的 schema 1 备份并迁移，也兼容 V0.6 的 schema 2 备份；没有为了版本号无必要升级备份 schema，也不会修改用户选择的旧备份文件。

非媒体导入均在读取前执行固定限制：报表最大 8 MB、工作区备份最大 2 MB、本机执行结果最大 2 MB、转写文本最大 4 MB、分析交接包最大 128 KB。CSV 同时限制为最多 50,000 行、200 列，拒绝未闭合引号、空白表头和重复表头；JSON 或文本格式错误时不会覆盖当前成功状态。下一版任务 CSV 导出会把可能被电子表格解释为公式的文本单元格转为安全文本，不会执行其中内容。

## 自有母版匹配边界

母版匹配仅使用 SHA-256 文件指纹、文件大小和规范化文件名，在用户主动选择的团队自有或已授权目录中寻找候选源文件。它只用于证明或寻找用户有权使用的源文件，不检测、移除、破解、定位或规避平台水印、暗水印、来源标记与平台风控。

需要干净素材时，请使用团队自有原片、原始工程或获得明确授权的素材重新制作。

母版匹配只接受受支持的视频或图片。单轮最多 40 个平台素材与 120 个团队母版，单文件最多 256 MB、合计最多 1 GB；任一文件格式、大小或数量不合规时整轮拒绝。匹配过程可以取消；取消或失败会清空当前结果，不保留可导出的半成品。

## 自有或授权图片局部修复

- 只允许处理用户自己拍摄、制作，或已取得明确编辑授权的 PNG、JPEG、静态 WebP 图片。
- 没有勾选授权确认或没有用户手动画出的 Mask 时，扩展拒绝处理。Mask 可由指针画笔绘制，也可由用户使用键盘明确填写矩形范围；两种方式都不会自动定位内容。
- 程序不自动寻找文字、Logo、水印、品牌标识或任何平台标识，只修改手动 Mask 内的像素及其向内羽化区域。
- Mask 外像素保持不变；PNG 导出保留原透明通道，JPEG 默认质量不低于 `0.92`。
- 原图始终保留，导出文件使用新文件名。用户应优先回到自有原片或剪辑工程重新导出，局部修复只用于找不到原工程时的小范围普通瑕疵。
- 为降低页面崩溃风险，文件、尺寸、总像素、预计内存、Mask 比例和 Mask 绝对像素数都有上限。算法目前同步运行，大图仍可能导致短暂卡顿，应先缩小或分区处理。

## 自有母版转码

- 只有用户勾选确认“所选文件为团队自有或已获得明确转码授权的原片”后，扩展才会生成转码任务。
- 浏览器不会把视频上传到开发者或第三方服务器，也不会自动启动本机程序。
- 扩展生成的 JSON 任务清单包含用户主动填写的本地原片根目录、输出目录、文件名和编码参数。该文件只由用户主动导出，应像其他本地生产资料一样在可信设备上保管。
- 随项目提供的 `tools/transcode-worker.ps1` 在用户主动运行后调用用户本机的 FFmpeg，不进行网络访问，不删除原片，不覆盖已有输出。
- 输出固定为 MP4 / H.264 / AAC；可调整等比缩放边界、帧率、视频质量或码率、音频码率和采样率。
- 任务固定使用 `-map_metadata 0` 与 `-map_chapters 0`，尽可能保留目标容器可表示的元数据和章节。容器能力不同可能导致部分私有字段无法迁移。
- 不提供清元数据、裁剪、模糊、去标识、自定义滤镜、暗水印检测、暗水印移除或平台风控规避选项，也不声称转码会移除任何标记。
- 侧边栏单批最多 100 个视频、单文件 20 GB、合计 100 GB；素材处理与分析工作台单批最多 40 个视频、单文件 10 GB、合计 40 GB。格式、数量、单文件或总量超限时不生成清单，也不改变原始文件。

本地执行器完成后会生成结果 JSON，其中包含任务编号、输出路径、退出码和失败原因。扩展只在用户主动选择该结果文件后于当前会话读取并展示，不会上传或长期保存该文件。

## 素材处理与分析工作台及本地转写

- 工作台只对用户主动选择并确认拥有或获得授权的本地视频生成标准化 MP4 与 PCM WAV 任务。
- 抖音原页链接是可选来源备注。扩展不会请求、解析或下载该 URL；只有用户点击“只打开原页”后，浏览器才会像普通链接一样访问抖音，此时该网站会按其自身隐私政策接收常规网络信息。
- 来源 URL 会随本机路径一起写入用户主动导出的任务清单，因此该文件可能包含敏感生产信息，应只保存在可信设备。
- 浏览器页面不会直接启动 FFmpeg 或 whisper.cpp。用户必须主动导出、审查并在本机运行任务。
- whisper.cpp 任务只允许用户填写 Windows 绝对路径，并仅接受名为 `whisper-cli` 或 `whisper-cli.exe` 的本地执行文件与 `.bin` 模型；参数由扩展固定生成，不接受任意命令或自定义参数。
- 扩展也允许用户导入本机生成的 TXT、MD、SRT、VTT。文本只在页面内以确定性关键词规则进行结构标注，不上传、不调用远程模型，也不预测投放效果。
- “发送并打开编导台”和备用 JSON 使用同一固定内容 schema 与不可逆短校验标识。侧边栏拒绝错误版本、错误来源或方法、未知字段、超长文本、本机路径、活动内容、篡改标识和本次会话中已成功应用的交接包。
- 会话交接先校验固定 envelope、大小、来源与时间，再校验内部交接包；`chrome.storage.onChanged` 只用于响应当前扩展写入的 session 槽，不读取网页、账号或平台内部数据。Chrome 114/115 或 `sidePanel.open()` 失败时，结果仍可排队，界面会如实提示用户点击扩展图标。
- 交接包校验成功后只显示预览，不修改创作任务。已有不同文件或会话预览时不会自动覆盖，只显示待确认提示，由用户主动切换。只有用户勾选建议并点击确认后才填入仍为空白的字段；已有内容不会被覆盖。应用成功或明确清除会话结果后才移除 session 槽；备用 JSON 流程继续可用。

## 平台标识与来源边界

本项目不检测、不定位、不破解、不移除平台隐藏水印、暗水印、版权标记、溯源指纹或风控标识，也不提供批量水印处理、抖音链接无水印下载、媒体地址解析、请求拦截或规避平台机制。转码、音频提取和手动图片局部修复均不声称会移除任何平台标识。
