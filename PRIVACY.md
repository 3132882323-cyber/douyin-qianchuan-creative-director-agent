# 隐私说明

## 本地处理

- 商品 Brief、复盘摘要、创意策略和用户编辑后的生产资料保存在当前浏览器的 `chrome.storage.local`。
- 原始 CSV、视频、图片和母版文件只在用户主动选择后于本地浏览器内读取与计算，不上传到服务器。
- 扩展不会自动浏览本地文件，也不会把原始素材内容写入扩展存储。
- 用户可以通过移除扩展或在浏览器扩展设置中清除数据，删除本地工作区内容。

## 不会进行的操作

- 不读取或保存千川账号密码。
- 不拦截千川或抖音页面的网络请求。
- 不向网页注入脚本，不使用远程托管代码。
- 不向开发者、第三方分析服务或广告平台发送使用数据。

## 版本检查

- 默认不会访问 GitHub。用户点击“检查新版本”或明确开启“打开侧边栏时自动检查”后，浏览器才会请求本项目的公开 GitHub Release 元数据。
- 首次检查需要用户授予 `https://api.github.com/*` 可选主机权限；未授权时不会发起网络请求。
- 请求不包含商品 Brief、投放报表、脚本、素材、文件名、Cookie、Token 或扩展签名私钥。
- GitHub 会像处理普通网络访问一样接收 IP 地址和标准请求信息；具体处理方式受 GitHub 隐私政策约束。
- Release 返回内容只用于比较版本号并提供项目发布页地址，不会作为脚本执行，也不会自动下载或安装文件。
- 自动检查最多每天一次，并且只在侧边栏被打开时运行。用户可以随时关闭。

工作区备份只由用户主动导出到本地，包含商品 Brief、复盘摘要、拍摄方案和更新设置；不包含原始 CSV、视频、图片、账号凭据或私钥。导入备份前会再次要求用户确认覆盖当前工作区。

## 自有母版匹配边界

母版匹配仅使用 SHA-256 文件指纹、文件大小和规范化文件名，在用户主动选择的商家自有目录中寻找候选源文件。它只用于证明或寻找用户拥有的源文件，不检测、移除、破解、定位或规避平台水印、暗水印、来源标记与平台风控。

需要干净素材时，请使用商家自有原片、原始工程或获得明确授权的素材重新制作。
