# 隐私说明

## 本地处理

- 可选创作任务、复盘摘要、创意策略和用户编辑后的生产资料保存在当前浏览器的 `chrome.storage.local`。
- V1.4.0 的本地项目集合、测试版本、可选人工制作状态、结果和可选人工决策记录保存在同一扩展来源的 IndexedDB。最多 20 个项目；每个项目最多 500 个测试版本和 500 条结果。它们不会同步到其他浏览器或账号。
- 人工制作状态只保存编导主动选择的固定代码（待拍、拍摄中、剪辑中、待投放、已上线或已搁置）和更新时间。未选择时保存为空并显示“未标记”；扩展不会根据生成、复制、导出、文件选择或结果导入推断制作状态。
- 项目切换先保存当前可恢复工作区，再写入待切换项目标记并刷新侧边栏。未完成的报表标签编辑、素材匹配和图片修复仍属于侧边栏页面内存，切换前会明确提示且需要用户确认；视频处理队列只存在于独立工作台页面，不参与侧边栏项目切换。
- 测试结果只来自用户主动填写的单版本表单，或用户主动选择的 CSV / TSV。两种方式都按测试编号匹配并使用相同的数值、比例、漏斗和口径校验；手动选择已有版本时只在表单中预载已保存指标，输入原字符串不会另外留存。扩展不保存结果文件、原始行或文件名；未匹配编号在预览中列出且不会写入。相同编号的旧结果只在用户再次确认后替换。点击量大于展示量、转化量大于点击量等不可能漏斗会被拒绝；ROI、CTR 或 CVR 与基础指标差异较大时，只保存固定的本地口径提醒代码，不保存对应原始行。
- “实验下一步”只根据当前项目已有的版本、人工制作状态、结果、固定口径提醒和人工决策状态在页面内计算。只有编导明确标记为“已上线”且没有结果的版本会触发主动回填提醒；其他状态仍允许用户主动补录历史结果，但不会被系统催回填。
- 顶部“今日编导行动台”只把当前流程状态、“实验下一步”和最新批次的父子可拍内容 Diff 排序为最多 3 项。队列、优先级、点击记录和内容风险汇总均只存在于当前页面内存；人工制作状态本身只有在用户更改版本卡下拉框时才写入。点击行动不会自动回填、保存人工决策、生成方案或执行平台操作。
- 下一轮父版本建议同样只在页面内计算，只考虑证据仍有效的“保留方案”或“继续测试”人工结论；“停止测试”和待重审结论会被排除。建议本身不写入存储，只有用户点击并随后生成新方案时，父版本关系才会进入新的本地版本记录。
- “开拍准备”只在当前页面内检查本地方案的必需现场字段是否为空或仍为待补占位文案；结果不写入存储，不评价创意质量，也不自动生成或修改字段。复制不完整清单时仅显示浏览器确认框，取消后不会写入剪贴板或改变制作状态。
- “复制开拍清单”和“复制完整方案”只在用户点击后，把当前本地方案的派生文本写入操作系统剪贴板。开拍清单只是按基线优先和变体编号重排已有字段，不新增推断、上传、网络请求或制作状态写入；剪贴板内容由操作系统和用户后续粘贴目标管理。
- “前三秒现场监看卡”只从用户当前选中方案的测试变量、受众、场景、首帧分镜、钩子、口播首句、首屏字幕、证据任务和剪辑要求派生。所有检查均在页面内存完成，不保存监看结论或复制记录；只有用户点击按钮后才写入剪贴板。信息密度阈值只触发人工停帧/盲读提醒，不自动评价创意质量、识别人物或分析视频画面。
- “前三秒拍前盲审”只在页面内存中对当前 2–12 条同批本地方案稳定打乱。公共盲审包会移除已知测试/批次编号、基线素材名、金额和带数值的 ROI、CTR、CVR、CPA、GMV、消耗、转化率、点击率、完播率或留存率；导演映射使用独立按钮写入剪贴板，绝不进入公共包。盲审编号、顺序、映射、反馈和复制记录均不保存；修改任一方案后编号会变化。
- “导演合议单”只在用户点击后，把同一盲审编号下的匿名字段和私有映射重排为人工填写模板并写入剪贴板。扩展不读取评审者身份、不接收或保存票数与自由反馈、不按多数票自动选优，也不会因合议单复制而修改方案、父版本、制作状态或投放状态。
- “批次共用镜头板”和“批次剪辑装配单”只在当前页面内读取同批本地方案的 B00 编号、唯一变量、钩子、主张、场景、受众、保持不变项和现有生产资料，用固定规则区分共用镜头、变量插拍和逐版本时间轴替换范围。它们不读取或分析视频画面、不扫描素材文件夹、不估算工时、不自动移动、改名、合并或导出素材，也不操作剪辑软件；派生文本、检查结果和复制记录均不保存，只有用户点击后才写入操作系统剪贴板。固定字段漂移、变量值重复、声明与字段不一致或现场资料缺失时失败关闭，不自动修改方案或制作状态。
- 实验版本的结果/制作状态筛选、批次筛选、搜索词和页码只在当前侧边栏页面内计算，不写入长期存储、工作区备份或台账导出。搜索只匹配测试编号、父版本、批次、主要变量、基线素材和方案变量值，不索引人工决策理由；分页不改变或截断已保存记录。
- 版本卡上的回填、决策和父版本按钮只定位当前项目中已经显示的测试编号。回填按钮仅预载本地表单，仍需预览和再次确认才写入；决策按钮只展开人工表单；父版本按钮只在证据仍有效的“保留方案”或“继续测试”结论上显示，点击只选中，不生成新方案。
- 父子指标对照只使用当前项目 IndexedDB 中已保存的版本关系、最低消耗和用户主动回填结果，在当前页面内计算，不新增存储记录或网络请求。父子任一方缺少结果、未达到各自最低消耗或存在指标口径提醒时不计算差值；显示的 ROI 与百分点变化仅为描述性对照，不代表因果、统计显著性或胜负。
- 父子可拍内容 Diff 同样只在当前页面读取两个本地版本中已经保存的白名单方案字段，不访问原始素材或平台。界面最多展示每个变化字段前 800 个字符；Diff 和单变量提醒不写入存储、备份或台账，不自动修改方案，也不把生产资料的连带变化直接认定为第二个创意变量。
- “千川投放交接卡”只在当前页面读取已保存的版本白名单字段、人工制作状态、项目目标 ROI 和最低消耗。变体只与同批次唯一 B00 基线比较，跨轮基线只与明确父版本比较；批次缺失、基线缺失或存在歧义时失败关闭并要求人工核对，不会跨批次猜测对照版本。
- 交接卡只在用户点击后写入操作系统剪贴板；有缺项时会先显示确认框，取消后不写剪贴板。完整度、缺项和单变量核对结果不写入项目、工作区、IndexedDB、备份或台账，也不会修改人工制作状态。剪贴板内容由操作系统和用户后续粘贴目标管理。
- “最新批次投放交接”只按本地版本的方案生成时间选择一个最新批次，并在页面内按 B00、其他基线、编号变体和其他版本排序。整批交接包复用上述单条检查结果，不读取筛选器、平台状态或账号数据；缺项时要求用户再次确认，超过 40 个版本时拒绝复制。批次选择、排序、就绪汇总和复制记录均不持久化。
- “灵感接力”复用上述最新批次选择，仅在页面内用固定关键词规则把用户自有或已授权方案抽象为通用创意机制。相近机制会去重，最多显示 6 张；规则不会调用远程模型、联网搜索、训练个性化模型或读取其他项目。灵感卡对象不保留原测试编号、批次编号、素材名、原变量文本、脚本、项目名或指标数值。
- 只有用户点击“复制脱敏灵感包”或单张卡片的“复制共创挑战”时才写入操作系统剪贴板。整包文本只包含通用机制、启发问题、原创改写动作和非数值证据状态；共创挑战只接收已脱敏的机制、证据代码，并用仓库内固定定义重新生成三路发散和交付要求，不信任或输出卡片自由文本。两者都明确要求至少改变受众、场景、证据、节奏与行动引导中的三项。复制记录、灵感卡、挑战题和分类结果不进入工作区、IndexedDB、备份或台账。
- 人工决策只由用户主动填写并保存，包含继续测试/保留方案/停止测试、主指标、护栏指标、最多 600 字的理由、决策时间和证据快照。证据快照只包含结果导入时间、描述性状态、目标 ROI、最低消耗和不可逆短指纹；结果或阈值变化后旧决策标记为需重新确认，不会自动改写用户结论。
- 首次三步引导是否已收起只保存为一个本地布尔设置；用户可以随时从“隐私与产品边界”重新打开引导。
- 自 V1.0.0 起，从 V0.5 升级时旧版创作资料会一次性映射为新的创作任务；V0.6 的当前结构可直接恢复。旧原值仅暂存在当前浏览器的本地迁移归档中，不参与新方案生成，也不进入新备份；只有迁移结果成功写回后才清理旧存储键。
- 工作区启动采用逐键恢复。损坏记录会被隔离并在界面列出具体存储键，不会导致整个侧边栏空白；扩展不会自动删除这些键，只有用户显式点击并再次确认后才清理。
- 原始 CSV、视频、图片和母版文件只在用户主动选择后于本地浏览器内读取与计算，不上传到服务器。
- 扩展不会自动浏览本地文件，也不会把原始素材内容写入扩展存储。
- 唯一的视频处理队列只保留在当前独立工作台页面中；侧边栏不再维护第二份转码队列，扩展不会把视频内容放入 `chrome.storage.local`。
- 图片局部修复的原图、Mask 与结果只保留在当前侧边栏页面内；扩展不会覆盖原图，只有用户点击导出后才创建新文件。
- 素材处理与分析工作台中的视频列表、处理清单、转写文本、时间/段落映射、结构分析和可拍任务草稿只保留在当前工作台页面内，除非用户主动导出任务或结果文件。
- TXT、MD、SRT、VTT 与手动粘贴文本只在当前页面内规范为 Transcript v2。它包含来源类型、格式、语言、分段、时长、解析器版本、解析提醒和不可逆短内容指纹；该指纹只用于本页映射一致性判断，不是文件身份或版权证明。用户编辑正文后，旧来源映射会明确失效，不会继续与新文字错误关联。
- 工作台只有在用户生成、逐项核对并明确确认当前草稿后，才会把 V2 交接写入当前浏览器的 `chrome.storage.session`，或按用户点击创建本地 JSON。V2 包含固定分析摘要、四类短建议和最大 48 KB 的白名单可拍草稿，不包含原始转写全文、来源文件名、视频、账号数据、Cookie 或本机路径。页面离开提醒只读取当前内存状态，不写入任何存储。未附草稿的旧 V1 包继续兼容。
- V1.4.0 的“处理本地视频 / 分析已有转写”入口、顶部唯一主操作、阶段、完成度和集中错误都只根据当前页面内存计算，不写入长期工作区。入口切换不会清空已选视频、处理队列、转写正文、分析结果或草稿；这些内容仍不持久化。存在未导出任务、仅存在页面内的手动正文、未保存分析或草稿时，刷新或关闭会触发浏览器原生提醒，但不会自动保存或被描述为可恢复任务；可从仍在本机的导入文件重新建立的正文不单独触发提醒。
- 工作台“成果去向”只读取上述当前页面内存中的任务导出、正文来源一致性、分析导出、草稿导出、人工确认和会话交接状态。它不新增行为记录、时间戳、长期存储或网络请求；“已导出”只表示浏览器已创建下载，“已确认”不等于已发送，“已发送”仍需侧边栏人工预览。
- “分析已有转写”被标为推荐快速路径时，只会在页面上暂时隐藏本轮无关的视频与 FFmpeg 区块；切换入口即可重新显示，不会删除或上传被隐藏的页面态。
- 工作台为转写导入、执行结果导入和交接发送分配短生命周期内存令牌。新操作、任务重建或本轮重置会使旧令牌失效，旧异步结果不得再写回界面；令牌不进入 `chrome.storage`、备份或导出文件，也不记录用户行为。
- 用户点击“开始新一轮 / 重置本轮”时，扩展先列出将被清空的当前页面内容，并标明尚未导出的任务、分析或草稿；取消确认不会修改状态。确认后只复位当前工作台的文件引用、路径与授权参数、任务、转写正文、分析、草稿、本页交接状态和界面提示，不调用存储删除 API，不删除本地原文件、已下载文件、侧边栏数据、`chrome.storage.local` 工作区或已发送到会话收件箱的结果。
- 会话交接使用固定单槽 envelope，并在两小时后过期；相同结果重复发送保持幂等，不同待处理结果不会静默覆盖。坏数据和过期数据失败关闭并清理，应用成功或用户明确清除后才消费该槽。会话交接不进入 `chrome.storage.local`、工作区备份或长期任务历史。
- 行动台中的核心流程动作只根据当前项目已有记录计算继续位置；待拍、拍摄中、剪辑中、待投放、已上线和已搁置只读取编导主动保存的制作状态，不从其他记录推断，也不会声称可以恢复独立工作台的页面会话。
- 下一版任务会保存一个不可逆的短依赖指纹，用于判断字段映射、复盘排名、标签、ROI、创作任务和生成参数是否变化。它不是文件指纹，也不包含原始 CSV 或素材内容；旧方案缺少该字段时会安全要求重新生成。
- 用户可以通过移除扩展或在浏览器扩展设置中清除站点/扩展数据，删除 `chrome.storage.local` 与 IndexedDB 中的本地工作区内容。

## 不会进行的操作

- 不读取或保存千川账号密码。
- 不拦截千川或抖音页面的网络请求。
- 不向网页注入脚本，不使用远程托管代码。
- IndexedDB 使用固定在仓库内的 `idb 8.0.3`（ISC）Promise 封装；运行时不从 CDN 或包仓库下载代码。
- 扩展页使用 Manifest V3 CSP `script-src 'self'; object-src 'none';`，不允许运行远程脚本或对象插件。
- 不向开发者、第三方分析服务或广告平台发送使用数据。
- 不通过抖音链接解析或下载视频，不调用云端转写，不使用浏览器可能联网的语音识别接口。
- 不读取千川投放结果，不自动同步平台数据，不根据结果自动发布素材；版本状态只是最低消耗、目标 ROI 与父版本的描述性对照，不构成统计显著性或因果结论。
- 不通过投放交接卡创建广告计划、设置或修改预算、启停投放、读取账户数据、验证平台执行状态或预测投放效果。
- 不把灵感卡或共创挑战描述为爆款公式、版权许可、效果保证或因果结论，也不利用它复制他人素材、复原原脚本或输出具体品牌主张。
- 实验台账只在用户点击后导出当前项目的版本关系、人工制作状态与更新时间、描述性结果、回填指标、固定口径提醒和用户主动保存的人工决策；不包含原始报表、结果文件名、素材、完整拍摄脚本、账号数据或本机路径。CSV 文本字段执行公式安全处理。

## 版本检查

- 默认不会访问 GitHub。用户点击“检查新版本”或明确开启“打开侧边栏时自动检查”后，浏览器才会请求本项目的公开 GitHub Release 元数据。
- 首次检查需要用户授予 `https://api.github.com/*` 可选主机权限；未授权时不会发起网络请求。
- 请求不包含创作任务、投放报表、脚本、素材、文件名、Cookie、Token 或扩展签名私钥。
- GitHub 会像处理普通网络访问一样接收 IP 地址和标准请求信息；具体处理方式受 GitHub 隐私政策约束。
- Release 返回内容只用于比较版本号并提供项目发布页地址，不会作为脚本执行，也不会自动下载或安装文件。
- 自动检查最多每天一次，并且只在侧边栏被打开时运行。用户可以随时关闭。

工作区备份只由用户主动导出到本地。V1.4.0 继续使用兼容的 schema 3，包含经过结构白名单筛选的当前工作区、所有本地项目、测试版本、可选人工制作状态、结果记录、可选人工决策和更新设置；未知字段不会进入新备份。V1.3 及更早版本缺少制作状态时可直接导入并显示“未标记”。备份不包含原始 CSV、媒体、账号凭据、私钥、会话交接或本机路径。

非媒体导入均在读取前执行固定限制：报表最大 8 MB、全部项目工作区备份最大 12 MB、测试结果最大 2 MB / 1,000 行、本机执行结果最大 2 MB、转写文本最大 4 MB、分析交接包最大 128 KB。素材报表 CSV 仍限制为最多 50,000 行、200 列；所有 CSV 拒绝未闭合引号、空白或重复表头，JSON 或文本格式错误时不会覆盖当前成功状态。下一版任务 CSV 导出会把可能被电子表格解释为公式的文本单元格转为安全文本，不会执行其中内容。

## 自有母版匹配边界

母版匹配仅使用 SHA-256 文件指纹、文件大小和规范化文件名，在用户主动选择的团队自有或已授权目录中寻找候选源文件。它只用于证明或寻找用户有权使用的源文件，不检测、移除、破解、定位或规避平台水印、暗水印、来源标记与平台风控。

需要干净素材时，请使用团队自有原片、原始工程或获得明确授权的素材重新制作。

母版匹配只接受受支持的视频或图片。单轮最多 40 个平台素材与 120 个团队母版，单文件最多 256 MB、合计最多 1 GB；任一文件格式、大小或数量不合规时整轮拒绝。匹配过程可以取消；取消或失败会清空当前结果，不保留可导出的半成品。

## 自有或授权图片局部修复

- 只允许处理用户自己拍摄、制作，或已取得明确编辑授权的 PNG、JPEG、静态 WebP 图片。
- 没有勾选授权确认或没有用户手动画出的 Mask 时，扩展拒绝处理。Mask 可由指针画笔绘制，也可由用户使用键盘明确填写矩形范围；两种方式都不会自动定位内容。
- 程序不自动寻找文字、Logo、水印、品牌标识或任何平台标识，只修改手动 Mask 内的像素及其向内羽化区域。
- Mask 外像素保持不变；PNG 导出保留原透明通道，JPEG 默认质量不低于 `0.92`。
- 原图始终保留，导出文件使用新文件名。用户应优先回到自有原片或剪辑工程重新导出，局部修复只用于找不到原工程时的小范围普通瑕疵。
- 为降低页面崩溃风险，文件、尺寸、总像素、预计内存、Mask 比例和 Mask 绝对像素数都有上限。算法目前同步运行，大图仍可能导致短暂卡顿，应先缩小或分区处理。

## 本地视频处理

- 只有用户勾选确认“所选文件为团队自有或已获得明确转码授权的原片”后，扩展才会生成转码任务。
- 浏览器不会把视频上传到开发者或第三方服务器，也不会自动启动本机程序。
- 扩展生成的 JSON 任务清单包含用户主动填写的本地原片根目录、输出目录、文件名和编码参数。该文件只由用户主动导出，应像其他本地生产资料一样在可信设备上保管。
- 随项目提供的 `tools/transcode-worker.ps1` 在用户主动运行后调用用户本机的 FFmpeg，不进行网络访问，不删除原片，不覆盖已有输出。
- 输出固定为 MP4 / H.264 / AAC；可调整等比缩放边界、帧率、视频质量或码率、音频码率和采样率。
- 进阶输出参数只存在于当前工作台页面内；重置本轮或刷新页面会恢复默认值，不写入长期存储。固定的分析音频任务始终输出 PCM 16-bit、16 kHz、单声道 WAV，不受 MP4 音频设置影响。
- 任务固定使用 `-map_metadata 0` 与 `-map_chapters 0`，尽可能保留目标容器可表示的元数据和章节。容器能力不同可能导致部分私有字段无法迁移。
- 不提供清元数据、裁剪、模糊、去标识、自定义滤镜、暗水印检测、暗水印移除或平台风控规避选项，也不声称转码会移除任何标记。
- 唯一工作台队列单批最多 40 个视频、单文件 10 GB、合计 40 GB。格式、数量、单文件或总量超限时不生成清单，也不改变原始文件。

本地执行器完成后会生成结果 JSON，其中包含任务编号、输出路径、退出码和失败原因。扩展只在用户主动选择该结果文件后于当前会话读取并展示，不会上传或长期保存该文件。

## 素材处理与分析工作台及本地转写

- 工作台只对用户主动选择并确认拥有或获得授权的本地视频生成标准化 MP4 与 PCM WAV 任务。
- 抖音原页链接是可选来源备注。扩展不会请求、解析或下载该 URL；只有用户点击备注旁的“打开原页”后，浏览器才会像普通链接一样访问抖音，此时该网站会按其自身隐私政策接收常规网络信息。
- 来源 URL 会随本机路径一起写入用户主动导出的任务清单，因此该文件可能包含敏感生产信息，应只保存在可信设备。
- 浏览器页面不会直接启动 FFmpeg 或 whisper.cpp。用户必须主动导出、审查并在本机运行任务。
- whisper.cpp 任务只允许用户填写 Windows 绝对路径，并仅接受名为 `whisper-cli` 或 `whisper-cli.exe` 的本地执行文件与 `.bin` 模型；参数由扩展固定生成，不接受任意命令或自定义参数。
- 扩展也允许用户导入本机生成的 TXT、MD、SRT、VTT。文本会先规范成 Transcript v2；SRT/VTT 时间重叠只生成本地提醒，不会静默修改。文本及其时间/段落来源只在页面内以确定性关键词规则进行结构标注，不上传、不调用远程模型，也不预测投放效果。
- 可拍任务草稿由固定本地模板生成，只允许一个主要测试变量，并保存匿名来源分析编号与本地测试编号。它不是效果预测；用户必须人工核对和确认，任何再次编辑都会取消旧确认。
- “发送草稿并打开编导台”和备用 JSON 使用受版本控制的 V1/V2 固定内容 schema 与不可逆短校验标识。侧边栏拒绝错误版本、错误来源或方法、未知字段、超长文本、本机路径、活动内容、篡改标识和本次会话中已成功应用的交接包。
- 会话交接先校验固定 envelope、大小、来源与时间，再校验内部交接包；`chrome.storage.onChanged` 只用于响应当前扩展写入的 session 槽，不读取网页、账号或平台内部数据。Chrome 114/115 或 `sidePanel.open()` 失败时，结果仍可排队，界面会如实提示用户点击扩展图标。
- 交接包校验成功后只显示预览，不修改创作任务。V2 草稿始终只读，不写入或覆盖 `creativePlan`。已有不同文件或会话预览时不会自动覆盖，只显示待确认提示，由用户主动切换。只有用户勾选旧四类建议并点击确认后才填入仍为空白的字段；已有内容不会被覆盖。应用成功或明确清除会话结果后才移除 session 槽；备用 JSON 流程继续可用。

## 平台标识与来源边界

本项目不检测、不定位、不破解、不移除平台隐藏水印、暗水印、版权标记、溯源指纹或风控标识，也不提供批量水印处理、抖音链接无水印下载、媒体地址解析、请求拦截或规避平台机制。转码、音频提取和手动图片局部修复均不声称会移除任何平台标识。
